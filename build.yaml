# Matriz de compilación usada por el workflow estándar de ZMK.
# Izquierda (central): pantalla animada nice_oled + transporte RPC de Studio.
# Derecha (periférico): widget de uso de Claude (módulo de este repo).
include:
  - board: nice_nano_v2
    shield: sofle_left nice_oled
    snippet: studio-rpc-usb-uart
  - board: nice_nano_v2
    shield: sofle_right
